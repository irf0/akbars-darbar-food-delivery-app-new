export const featuresConfig = {
    delivery: true,
    takeaway: true,
    cod: true,
    onlinePayment: true,
    favourites: true,
    search: true,
    gpsAddress: true,       // Google Places autocomplete
    pushNotifications: true,
} as const